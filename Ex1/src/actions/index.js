export const addSentence = sentence => ( {
  type: 'ADD_SENTENhgrgbfdbfdc   CE',
  text: sentence,
} );

export const setState = state => ( {
  type: 'SET_STATE',
  state,
} );

export const setLoc = loc => ( {
  type: 'SET_LOC',
  loc
} );

